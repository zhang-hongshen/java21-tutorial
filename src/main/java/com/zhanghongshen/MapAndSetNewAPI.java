package com.zhanghongshen;

import java.util.HashMap;
import java.util.HashSet;

public class MapAndSetNewAPI {

    public static void main(String[] args) {
        // pass the key-value pair num we need
        var map = HashMap.newHashMap(10);

        var set = HashSet.newHashSet(10);
    }
}
